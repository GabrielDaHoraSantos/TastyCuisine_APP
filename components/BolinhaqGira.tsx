
import { ActivityIndicator} from 'react-native';

export default function BolinhaqGira() {
    return(
        <ActivityIndicator size="large" color="#ffbb6e" style={{flex: 1, justifyContent: 'center', alignItems: 'center' }}/>
    )
}
